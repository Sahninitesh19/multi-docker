sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t niteshsahni/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true niteshsahni/react-test npm test

after_success:
  - docker build -t niteshsahni/multi-client ./client
  - docker build -t niteshsahni/multi-nginx ./nginx
  - docker build -t niteshsahni/multi-server ./server
  - docker build -t niteshsahni/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push niteshsahni/multi-client
  - docker push niteshsahni/multi-nginx
  - docker push niteshsahni/multi-server
  - docker push niteshsahni/multi-worker

